// Surf console — Tonight/Schedule. Re-exports the Brew page; business resolves
// to 'surf' from the x-business header (middleware tags /surf paths).
export { default } from '../../(admin)/admin/page'

export const dynamic = 'force-dynamic'
