// The Loop console — Tonight/Schedule. Re-exports the Brew page; business
// resolves to 'marines' from the x-business header (middleware tags /loop paths).
export { default } from '../../(admin)/admin/page'

export const dynamic = 'force-dynamic'
