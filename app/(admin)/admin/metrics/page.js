import MetricsClient from './MetricsClient'

export const metadata = { title: 'Metrics — The Loop' }
export const dynamic = 'force-dynamic'

export default function MetricsPage() {
  return <MetricsClient />
}
